# Gmail API
google-api-python-client
google-auth-httplib2
google-auth-oauthlib

# Article extraction
trafilatura
beautifulsoup4

# LLM (Ollama — just needs requests)
requests
python-dotenv

# TTS (Kokoro)
kokoro>=0.3; python_version == "3.12"
soundfile
numpy
